// These tests describe the App component functionality
describe.skip("The App component", () => {

  // 1. As a user, when I visit the app, I should see a rainbow on the dashboard
// - [ ] When user visits url, a homepage loads
// - [ ] home view includes header with navigation, main dashboard view with spectrum / rainbow, and footer with social media icon links

  it("Should allow a user to visit the home URL", () => {

  })
  
  it("Should render a header with navigation elements", () => {
  
  })

  it("Should render a dashboard with a section to display artwork", () => {

  })

  it("Should render a footer with social media links and copywrite dates", () => {

  })

})