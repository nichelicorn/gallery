// Gallery component
// Renders an artwork based on the hue selected by user

import React from "react";

function Gallery(props) {
  console.log("Gallery props <>>>", props);

  // In the first iteration, this component will accept props from the App, and be rendered conditionally
  // When a user clicks on a ColorCard, a condition will update and this component will replace the Spectrum in the dashboard view
}

export default Gallery;